services:
  web:
    build:
      context: ./out
      dockerfile: apps/web/Dockerfile
    ports:
      - "3000:3000"
    restart: always

  api:
    build:
      context: ./out
      dockerfile: apps/api/Dockerfile
    ports:
      - "4000:3000"
    restart: always
    env_file:
      - .env.api
